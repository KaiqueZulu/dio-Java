package edu.kaique.operadores;

public class Operadores {

    // Operador de atribuição "="
    // Operadores aritmeticos + (adicao e concatenacao), - (subtracao), * (multiplicacao) e / (divisao)
    // ++(incremento), -- (decremento), ! (negacao), condicao ? se satisfaz : se nao satisfaz (operador ternario)
    // ==(igual a), != (diferente), > (maior que), >= (maior ou igual), < (menor que) e <= (menor ou igual)
    // && (operador logico E) e || (operador logico OU)
}
